#[derive(Debug, Clone, Copy)]
pub enum ProductSize {
    SMALL,
    MEDIUM,
    LARGE,
}

#[derive(Debug, Clone, Copy)]
pub enum Category {
    CLOTHING,
    EDIBLE,
    SHOES,
    AUTOMOBILE,
    OTHERS,
}
